# ForestChen 个人网站 · 工作手册

这份手册面向网站所有者，教你日常如何：

- 发布 / 修改 / 删除博客文章
- 上传摄影作品
- 部署更新网站
- 做日常维护与备份

> 技术背景：纯静态站点（HTML/CSS/JS），文章存在 Supabase 云端，照片放在项目目录里。
> 项目位置：`G:\Chen`（git 仓库，远程指向 GitHub）。

---

## 目录

1. [本地预览](#一本地预览)
2. [发布博客文章](#二发布博客文章)
3. [上传摄影作品](#三上传摄影作品)
4. [更新部署网站](#四更新部署网站)
5. [绑定域名](#五绑定域名)
6. [日常管理与备份](#六日常管理与备份)
7. [常见问题 FAQ](#七常见问题-faq)
8. [常用命令速查](#八常用命令速查)

---

## 一、本地预览

在修改前先在本地看效果，不需要每次改完就上传。

**方法：直接用浏览器打开文件**

在资源管理器中双击 `G:\Chen\index.html`，或右键用浏览器打开即可预览。

> 注意：文章是从 Supabase 云端读取的，本地预览时联网即可看到线上文章；
> 照片如果在本地，`img/photos/` 里的图能正常显示；远程图需要联网。

---

## 二、发布博客文章

文章存在 Supabase 云端，**任何浏览器打开网站都能看到同一份**。

### 2.1 准备工作（只需做一次）

发布前先登录。打开网站后按 **F12** → 切到 **Console**，输入：

```js
await ForestChenAPI.signIn('你的邮箱', '你的密码');
```

- 登录状态会被浏览器记住，**同一个浏览器以后不用再登录**
- 换了浏览器/设备，需要再登录一次
- 忘记要不要登录：直接试发布，报错提示登录就说明没登录

### 2.2 发布新文章

在 Console 执行：

```js
await ForestChenAPI.publishArticle({
  title: '文章标题',
  date: '2026-08-06',      // 建议当天日期，格式 YYYY-MM-DD
  summary: '一句话摘要（可选）',
  content: '# 正文\n\n支持 **Markdown** 格式。\n\n- 列表\n- 列表\n\n> 引用'
});
```

成功后首页自动出现这篇文章，无需刷新。

**Markdown 常用语法：**

| 用途 | 写法 |
|---|---|
| 一级标题 | `# 标题` |
| 二级标题 | `## 标题` |
| 加粗 | `**文字**` |
| 斜体 | `*文字*` |
| 列表 | `- 项目` |
| 图片 | `![说明](图片地址)` |
| 代码块 | ```` ```代码``` ```` |
| 引用 | `> 引用内容` |
| 链接 | `[文字](链接地址)` |

### 2.3 修改 / 删除文章

删文章：先查文章 id，再删除。

```js
// 查看当前所有文章的 id 和标题
JSON.parse(localStorage.getItem('fc_articles')).map(a => ({ id: a.id, title: a.title }))
```

```js
// 删除指定 id 的文章
await ForestChenAPI.deleteArticle('文章id');
```

- 目前**没有网页端编辑/删除界面**，需要改内容就删除后重新发布，或在上面那条查 id 的语句基础上改字段再 `publishArticle`。
- 如果想在网页上直接点按钮发文章（不用控制台），我可以再给你加一个后台页面。

### 2.4 退出登录

```js
await ForestChenAPI.signOut();
```

---

## 三、上传摄影作品

照片不走云端，是放在项目目录里的文件。流程：**放图片 → 登记路径 → 部署**。

### 3.1 把照片放进项目

1. 把照片文件复制到 `G:\Chen\img\photos\` 目录
2. 建议统一命名，例如 `photo01.jpg`、`photo02.jpg`（支持 jpg/png/webp）

### 3.2 登记照片路径

用文本编辑器（如 VS Code、记事本）打开 `G:\Chen\js\pages\photos.js`，找到：

```js
const PHOTO_URLS = [
  /* 用户替换为实际照片路径，例如 'img/photos/photo01.jpg' */
];
```

改成（每张一行，逗号结尾）：

```js
const PHOTO_URLS = [
  'img/photos/photo01.jpg',
  'img/photos/photo02.jpg',
  'https://example.com/remote-photo.jpg',  // 也支持远程 URL
];
```

保存后本地预览确认显示正常，再部署。

### 3.3 图片处理建议（重要）

照片墙会整页加载所有图片，**原图过大 = 打开慢 + 费流量**：

- 长边建议压缩到 **1600px 以内**，导出 **WebP 或 JPEG（质量 80）**
- 体积控制在 **每张 < 500KB** 为宜
- 常用免费工具：**Squoosh**（浏览器在线压缩）、Photoshop「导出为 WebP」、Mac 自带「调整大小」
- 目前照片放在 git 仓库里，仓库总量建议保持在 **1GB 以下**；图多了之后可改用对象存储（Cloudflare R2 / 阿里云 OSS）存图，照片墙填远程 URL

### 3.4 删除照片

把 `photos.js` 里对应那一行删掉，再删掉 `img/photos/` 里的文件即可。

---

## 四、更新部署网站

所有内容改完，最后一步都是**上传到服务器**。方法就是 git 提交并推送，线上（GitHub Pages / Vercel）会自动更新。

### 4.1 推送命令（每次更新后）

在项目目录 `G:\Chen` 打开终端（VS Code 内置终端或 PowerShell）：

```powershell
git add -A
git commit -m "更新说明：本次改了啥"
git push
```

等 1~3 分钟，线上生效。

- 提示 `git push` 需要输入 GitHub 用户名和 token（不是登录密码），token 在 GitHub → Settings → Developer settings → Personal access tokens 生成
- 首次推送前如果没配置过，先执行一次：
  ```powershell
  git config --global user.name "你的名字"
  git config --global user.email "你的邮箱"
  ```

### 4.2 查看线上是否更新

- GitHub Pages：打开你的 `https://<用户名>.github.io`（或已绑定的域名）
- Vercel：打开 `forest-chen.vercel.app`
- 刷新时建议强制刷新（Ctrl+F5）避免缓存旧版

---

## 五、绑定域名

有自己域名（如 `forestchen.com`）时：

1. GitHub 仓库 → **Settings → Pages → Custom domain** 输入你的域名，点 Save
2. 去域名服务商（阿里云/腾讯云/GoDaddy 等）DNS 添加记录：
   - 类型 **CNAME**，主机记录 `@` 或 `www`，值 `<你的用户名>.github.io`
3. 勾选 **Enforce HTTPS**，等证书自动签发（可能几分钟到几小时）

---

## 六、日常管理与备份

- **文章备份**：文章在 Supabase 云端，已是「异地存储」。也可以定期在 SQL Editor 里 `select * from articles;` 导出留底
- **照片备份**：`G:\Chen\img\photos\` 就是源文件，定期把整个 `G:\Chen` 拷到移动硬盘即可
- **仓库体积**：定期用 `git status` 查看，避免把超大文件塞进仓库
- **安全**：发布权限已限定为只有你登录后能用；`supabase.js` 里的 URL/key 是公开的（客户端专用），泄露无妨，但不要放入其他密钥

---

## 七、常见问题 FAQ

| 问题 | 解决办法 |
|---|---|
| 发布报错「请先登录再发布」 | 执行 `await ForestChenAPI.signIn('邮箱','密码')` 后再发布 |
| 发布报错 401/403 | 检查 Supabase 的 RLS 策略是否执行了「仅登录者可发布」SQL |
| 页面文章是旧的/不显示 | 强制刷新（Ctrl+F5）；确认 Supabase 表里有数据 |
| 登录失败 | 确认账号是在 Supabase Authentication → Users 里创建的 |
| 照片不显示 | 检查 `img/photos/` 里文件名与 `photos.js` 里写的是否一致 |
| 本地预览看不到照片 | 照片墙图片来自本地目录时正常；远程 URL 需要联网 |
| git push 卡住/报认证 | 用个人访问 token 代替密码；确认已 `git config user.email/name` |

---

## 八、常用命令速查

```powershell
# 查看改动
git status

# 提交并上传（每次更新网站用这三条）
git add -A
git commit -m "说明"
git push

# 查看提交记录
git log --oneline
```
