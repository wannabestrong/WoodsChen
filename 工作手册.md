# OPEN 网站工作手册

这份文档用于日常维护 OPEN：发布文章、上传摄影作品、修改个人资料与 Music，并把更新部署到 GitHub。

## 1. 开始前

项目目录：

```text
D:\WoodsChen
```

启动本地预览：

```powershell
cd D:\WoodsChen
python -m http.server 4173
```

浏览器访问 `http://127.0.0.1:4173/#/archive`。不要直接双击 `index.html`，因为 ES module 和云端请求在 `file://` 下可能无法正常运行。

## 2. 发布文章

文章存储在 Supabase，发布成功后其他访客刷新网站即可看到，无需重新部署代码。

### 2.1 登录

打开网站，按 `F12` 进入 Console，执行：

```js
await ForestChenAPI.signIn('你的 Supabase 邮箱', '你的密码');
```

`ForestChenAPI` 是当前代码保留的兼容接口名。登录会话由 Supabase 保存在当前浏览器中；更换浏览器或设备后需要重新登录。

不要把邮箱密码写入代码、README、提交记录或截图。

### 2.2 发布一篇文章

```js
await ForestChenAPI.publishArticle({
  id: 'about-solitude',
  title: '关于孤独的十个片段',
  date: '2026-08-07',
  summary: '一段可选的简短摘要',
  content: `# 第一节

这里是正文，支持 **Markdown**。

## 第二节

- 列表项目
- 列表项目

> 一段引用

![图片说明](https://你的公开图片地址/image.jpg)`
});
```

注意：

- `id` 必须唯一，建议只用小写英文、数字和短横线。
- `date` 使用 `YYYY-MM-DD`。
- `content` 支持 Markdown。
- 云端文章会出现在 Archive 和 Notes。
- 当前首页封面会轮换使用项目里的 essay 占位图，并不会从 Supabase 上传独立封面。

### 2.3 查看文章 ID

```js
JSON.parse(localStorage.getItem('fc_articles') || '[]')
  .map(({ id, title, date }) => ({ id, title, date }));
```

### 2.4 修改文章

当前接口没有 `updateArticle`。推荐在 Supabase Dashboard 中修改：

1. 打开 Supabase 项目。
2. 进入 `Table Editor > articles`。
3. 找到对应 `id`，编辑 `title`、`date`、`summary` 或 `content`。
4. 保存后回到网站执行：

```js
await ForestChenAPI.loadArticlesFromServer();
```

也可以先删除旧文章，再使用相同 `id` 重新发布。

### 2.5 删除文章

```js
await ForestChenAPI.deleteArticle('about-solitude');
```

### 2.6 退出登录

```js
await ForestChenAPI.signOut();
```

## 3. 在文章正文中加入照片

Markdown 图片必须使用访客可访问的 HTTPS 地址。推荐使用 Supabase Storage：

1. 在 Supabase 打开 `Storage`，创建例如 `article-images` 的 bucket。
2. 如果图片需要公开显示，将 bucket 设为 public，或配置合适的读取策略。
3. 上传 WebP/JPEG 图片。
4. 复制图片的 public URL。
5. 在文章 Markdown 中写：

```markdown
![雨后的街道](https://你的项目.supabase.co/storage/v1/object/public/article-images/rain-street.webp)
```

不要使用本机路径，例如 `D:\Photos\photo.jpg`；其他访客无法读取你的电脑文件。

## 4. 更新 Archive 摄影卡片

摄影卡片目前是静态代码，更新后需要 Git 提交和部署。

### 4.1 准备图片

把图片放入：

```text
assets/archive/
```

推荐：

- 使用 WebP、JPEG 或 PNG。
- Archive 封面长边约 1600px 即可。
- 单张尽量控制在 1MB 内。
- 文件名使用小写英文和短横线，例如 `summer-river-cover.webp`。
- 保留原始照片备份，仓库内只放网页优化版本。

### 4.2 登记首页卡片

打开 `js/pages/home.js`，找到 `DEMO_ITEMS`，按现有格式添加或替换：

```js
{
  id: 'summer-river',
  type: 'photo',
  title: '夏日河岸',
  place: 'Hangzhou',
  date: '2026.08.07',
  image: 'assets/archive/summer-river-cover.webp',
  size: 'wide'
}
```

`size` 可使用：

| 值 | 用途 |
| --- | --- |
| `wide` | 横向卡片 |
| `hero` | 主要大图 |
| `tall` | 较高卡片 |
| `medium` | 标准卡片 |

保持 `id` 唯一。当前点击任意 `photo` 卡片都会打开统一的 Photo Story 页面，而不是每张卡片拥有独立详情页。

## 5. 更新 Photo Story

打开 `js/pages/photos.js`：

- `.photo-story-title`：摄影集标题。
- `.photo-story-meta`：地点和日期。
- `.photo-cover img`：摄影集主封面。
- `.photo-sequence`：序列图片。
- `.creation-note`：创作说明。

增加一张序列图片时，在 `.photo-sequence` 内加入：

```html
<figure class="photo-frame landscape" tabindex="0">
  <img src="assets/archive/你的图片.webp" alt="准确描述图片内容">
</figure>
```

竖图可把 `landscape` 改为 `portrait`。`alt` 应描述照片内容，不要写文件名。

## 6. 修改头像、简介和链接

头像文件：

```text
assets/avatar/avatar-replace-later.png
```

可以直接用同名文件覆盖，建议使用至少 `800x800` 的方图，并确保人物主体位于中心安全区域。

需要同步修改的文字位置：

- Archive 左栏简介与社交链接：`js/pages/home.js`
- About 页面简介与联系链接：`js/pages/about.js`
- 页脚年份：`js/pages/home.js`

替换示例链接时，确认 GitHub、Instagram、Email 和 RSS 都指向真实地址；未启用的链接建议暂时移除，避免保留 `href="#"`。

## 7. 启用 Music

当前 Music 是禁用占位状态。获得合法音源后，推荐：

1. 将自有或已获授权的 MP3 放在 `assets/audio/`，例如 `assets/audio/night-train.mp3`；或者使用允许跨域播放的稳定 HTTPS 音频直链。
2. 在 `js/pages/home.js` 的 `renderMusic()` 中加入 `<audio>` 或 `HTMLAudioElement`。
3. 连接播放、暂停、上一首、下一首和进度状态。
4. 移除按钮的 `disabled` 和“音源将在获得授权后启用”文案。
5. 在 Chrome、Safari 和手机浏览器测试播放。

不要抓取音乐平台受版权保护的流媒体地址，也不要把网页播放器页面 URL 当成 MP3 地址。

## 8. 发布代码更新

修改照片、页面文案或样式后：

```powershell
cd D:\WoodsChen
git status
git add -A
git commit -m "更新网站内容"
git push origin main
```

推送前检查：

- 网站本地预览正常。
- 图片路径和文件名大小写完全一致。
- `git status` 中没有 `.env`、密码、service role key 或临时文件。
- 不要提交未经授权的照片、音乐或字体。
- GitHub Pages 部署后使用 `Ctrl+F5` 强制刷新检查。

## 9. 常见问题

### 文章发布后别人看不到

- 在 Supabase `Table Editor` 确认数据已写入。
- 检查 `articles` 表是否启用了公开 `select` RLS policy。
- 打开浏览器 Console 查看网络或 RLS 错误。
- 执行 `await ForestChenAPI.loadArticlesFromServer()`。

### 发布时报 401 或 403

- 重新执行 `signIn`。
- 检查用户是否存在于 `Authentication > Users`。
- 检查 authenticated insert/delete policy。

### 照片在本地显示，线上不显示

- 检查文件是否已被 `git add` 和提交。
- 检查路径大小写；GitHub Pages 区分大小写。
- 路径必须相对于项目根目录，例如 `assets/archive/photo.webp`。

### 首页没有显示所有 Supabase 文章

当前 Archive 最多取 8 项，并将云端文章与静态摄影内容合并。这是现有设计限制；需要更多分页或完整归档时，应修改 `items()` 的数量策略并重新做响应式检查。

## 10. 备份

- Supabase：定期导出 `articles` 表。
- Storage：保留原图和上传文件的本地副本。
- Git：每次内容更新建立清晰提交。
- 密钥：只在密码管理器或安全环境变量中保存，不写进仓库。
